<% if params[:controller] == 'admin/posts' %>
    <%= form_tag(admin_posts_path, :method => 'get', id: 'search-form', class: 'navbar-form navbar-left search-form', role: 'search') do %>
        <div class="form-group">
          <%= text_field_tag :search, params[:search], placeholder: 'Search posts', class: 'form-control' %>
        </div>
        <%= submit_tag 'Search', :name => nil, class: 'btn btn-primary' %>
    <% end %>
<% elsif params[:controller] == 'admin/categories' %>
    <%= form_tag(admin_categories_path, :method => 'get', id: 'search-form', class: 'navbar-form navbar-left search-form', role: 'search') do %>
        <div class="form-group">
          <%= text_field_tag :search, params[:search], placeholder: 'Search categories', class: 'form-control' %>
        </div>
        <%= submit_tag 'Search', :name => nil, class: 'btn btn-primary' %>
    <% end %>
<% elsif params[:controller] == 'admin/users' %>
    <%= form_tag(admin_users_path, :method => 'get', id: 'search-form', class: 'navbar-form navbar-left search-form', role: 'search') do %>
        <div class="form-group">
          <%= text_field_tag :search, params[:search], placeholder: 'Search users', class: 'form-control' %>
        </div>
        <%= submit_tag 'Search', :name => nil, class: 'btn btn-primary' %>
    <% end %>
<% end %>